import { BRAND_COLORS } from "@/constants/BrandColors";

export const SHELL_COLORS = {
  primary: BRAND_COLORS.primary,
  primaryDark: BRAND_COLORS.primaryDark,
  primaryLight: "#3C74D6",
  accent: BRAND_COLORS.accentRed,
  white: BRAND_COLORS.white,
  textSecondary: BRAND_COLORS.textMuted,
  shadow: BRAND_COLORS.shadow,
  logoutRed: BRAND_COLORS.danger,
  drawerBg: BRAND_COLORS.primaryDeep,
  drawerItemBorder: BRAND_COLORS.drawerItemBorder,
  orange: BRAND_COLORS.primaryDark,
} as const;
